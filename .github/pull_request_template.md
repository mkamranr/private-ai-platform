**What this changes**

**What failure it prevents**

**Verification**
- [ ] `make check` passes
- [ ] Gates affected by this change pass — which:
- [ ] Docs updated in this PR (`docs/` is the contract)
- [ ] If it ships in the bundle: rebuilt and `make gate-phase8` run against the new one

**Anything reviewers should look at hardest**
